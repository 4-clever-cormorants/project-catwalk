import React from 'react';

const SizeSelector = () => (
  <label>
    size selector
    <select name="size" value="hi">
      <option>hi</option>
    </select>
  </label>
);

export default SizeSelector;
