// the qty selector should take the props of the sku currently selected by the qty selector ...

import React from 'react';

const QtySelector = ({ sku }) => (
  <div>
    hi
  </div>
);

export default QtySelector;
